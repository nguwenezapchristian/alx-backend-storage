#!/bin/bash

# The MongoDB query
query='db.school.find({ name: { $regex: /^Holberton/i } }).forEach(printjson);'

# Run the MongoDB query
# The database name will be passed as an option to the mongo command
mongo "$@" --quiet --eval "$query"